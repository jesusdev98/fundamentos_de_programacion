import type { Metadata } from "next";
import { PageIntro } from "@/components/learning/PageIntro";
import { sources } from "@/data/sources";

export const metadata: Metadata = { title: "Fuentes y créditos" };
export default function SourcesPage() { return <main><PageIntro eyebrow="Transparencia" title="Fuentes y créditos" description="El contenido educativo y sus ejemplos fueron redactados para este proyecto. Estas fuentes primarias se usaron para contrastar conceptos y decisiones técnicas." /><section className="page-container py-12"><div className="grid gap-5 md:grid-cols-2">{sources.map((source) => <article key={source.id} className="rounded-xl border border-slate-200 bg-white p-6"><p className="eyebrow">{source.type}</p><h2 className="mt-3 text-xl font-black text-slate-950">{source.name}</h2><p className="mt-1 text-sm text-slate-500">{source.organization}</p>{source.note ? <p className="mt-3 text-sm leading-6 text-slate-600">{source.note}</p> : null}{source.licensingNote ? <p className="mt-3 text-xs leading-5 text-slate-500">{source.licensingNote}</p> : null}<a className="mt-4 inline-block font-bold text-[#bd3f1d] underline" href={source.url} target="_blank" rel="noopener noreferrer">Abrir fuente</a></article>)}</div><p className="mt-10 rounded-lg bg-slate-100 p-5 text-sm leading-7 text-slate-600">Este proyecto no está afiliado, patrocinado ni aprobado por Mozilla, TC39, Ecma International, Node.js ni OpenJS Foundation. Los nombres pertenecen a sus respectivas organizaciones. Los enlaces no trasladan automáticamente la licencia de esas obras al contenido original de esta plataforma.</p></section></main>; }
