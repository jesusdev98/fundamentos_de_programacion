import { pythonLesson as lesson } from "../helpers.ts";

const tutorial = ["python-tutorial", "python-reference"] as const;
export const mediumLessons = [
  lesson("Medio", "python-medium-list-comprehensions", "List comprehensions", "comprehensions", ["Una list comprehension construye una lista declarando transformación, recorrido y filtros en una expresión legible."], 'cuadrados = [n * n for n in range(6) if n % 2 == 0]', ["La expresión inicial produce cada elemento.", "No fuerces lógica compleja dentro de una comprehension."], tutorial, "python-medium-list-comprehensions-exercise"),
  lesson("Medio", "python-medium-dict-set-comprehensions", "Comprehensions de dict y set", "comprehensions", ["Las comprehensions de diccionario generan pares clave-valor; las de conjunto eliminan duplicados según hash e igualdad."], 'longitudes = {texto: len(texto) for texto in ["sol", "luna"]}\niniciales = {texto[0] for texto in ["sol", "senda"]}', ["Una dict comprehension necesita clave y valor.", "Un set no conserva duplicados."], tutorial),
  lesson("Medio", "python-medium-function-values", "Funciones como valores y lambda", "funciones-como-valores", ["Las funciones son objetos: pueden asignarse, almacenarse en colecciones, recibirse como argumentos y devolverse. lambda crea una función anónima limitada a una expresión y resulta útil para criterios breves, no para ocultar lógica compleja."], 'operaciones = [str.lower, lambda texto: texto.strip()]\nresultado = operaciones[0]("PYTHON")', ["Pasa la función sin paréntesis cuando no quieras ejecutarla todavía.", "Prefiere def si la operación necesita nombre, documentación o varias expresiones."], ["python-functions", "python-reference"], "python-medium-function-values-exercise"),
  lesson("Medio", "python-medium-unpacking", "Unpacking y starred expressions", "unpacking", ["El unpacking distribuye elementos de un iterable entre destinos. Un único nombre con * recoge la parte variable en una lista."], 'primero, *centro, ultimo = [1, 2, 3, 4]\ncombinada = [0, *centro, 5]', ["La cantidad de destinos debe ser compatible.", "* también expande iterables al construir colecciones."], tutorial),
  lesson("Medio", "python-medium-args", "Argumentos variables con *args", "args", ["Un parámetro *args reúne argumentos posicionales restantes en una tupla. Permite adaptar una función sin perder el orden de llamada."], 'def sumar(*valores):\n    return sum(valores)', ["args es una convención; el asterisco define el comportamiento.", "No reemplaza un contrato de parámetros claro."], tutorial, "python-medium-args-exercise"),
  lesson("Medio", "python-medium-kwargs", "Argumentos nombrados con **kwargs", "kwargs", ["**kwargs reúne argumentos por palabra clave en un diccionario. Al llamar, ** expande un mapping con claves de texto válidas."], 'def configurar(**opciones):\n    return opciones.get("tema", "claro")', ["Las claves deben ser nombres de parámetro válidos.", "Valida opciones desconocidas cuando el contrato sea cerrado."], tutorial),
  lesson("Medio", "python-medium-functional-tools", "map, filter, zip y enumerate", "transformaciones-funcionales", ["map transforma elementos de forma perezosa y filter conserva los que cumplen un predicado. zip recorre iterables en paralelo hasta que termina el más corto; enumerate añade posiciones sin gestionar un contador manual."], 'nombres = ["Ada", "Lin"]\npuntos = [8, 9]\nfilas = list(enumerate(zip(map(str.upper, nombres), puntos), start=1))', ["map, filter, zip y enumerate devuelven iteradores en Python 3.", "Una comprehension suele ser más clara para una transformación sencilla; zip destaca al combinar recorridos."], ["python-iterators", "python-functions"], "python-medium-functional-tools-exercise"),
  lesson("Medio", "python-medium-legb", "LEGB, global y nonlocal", "legb", ["Python busca nombres en los ámbitos local, enclosing, global y builtins. global permite reasignar un nombre del módulo y nonlocal reasigna uno del ámbito envolvente más cercano; ambas declaraciones deben usarse con moderación porque introducen estado compartido."], 'total = 0\ndef registrar():\n    global total\n    total += 1\n\ndef contador():\n    valor = 0\n    def avanzar():\n        nonlocal valor\n        valor += 1\n        return valor\n    return avanzar', ["global apunta al ámbito del módulo; nonlocal, a una función envolvente.", "Prefiere devolver valores y pasarlos como argumentos cuando no necesites estado compartido."], tutorial, "python-medium-legb-exercise"),
  lesson("Medio", "python-medium-modules", "Imports, módulos y paquetes", "imports", ["Cada módulo tiene su propio espacio de nombres. import modulo conserva visible el origen; from modulo import nombre enlaza un miembro concreto y los imports relativos se resuelven respecto del paquete actual."], 'import math\nfrom pathlib import Path\nprint(math.isqrt(81), Path("datos"))', ["Los módulos cargados se reutilizan desde sys.modules.", "Evita from modulo import * y dependencias circulares entre módulos."], ["python-imports", "python-reference"]),
  lesson("Medio", "python-medium-classes", "Clases e instancias", "clases", ["Una clase define cómo construir objetos y qué comportamiento comparten. self es la instancia receptora y se pasa de forma implícita al llamar un método."], 'class Contador:\n    def __init__(self, inicio=0):\n        self.valor = inicio\n    def avanzar(self):\n        self.valor += 1\n        return self.valor', ["__init__ inicializa una instancia ya creada.", "Los atributos de instancia viven normalmente en __dict__."], tutorial, "python-medium-classes-exercise"),
  lesson("Medio", "python-medium-class-attributes", "Atributos de clase e instancia", "atributos", ["Un atributo de clase se comparte durante la búsqueda; asignar el mismo nombre en una instancia crea normalmente un atributo propio que lo sombrea."], 'class Documento:\n    categoria = "texto"\n\nuno = Documento()\nuno.categoria = "informe"', ["No guardes estado mutable por instancia en la clase.", "type(objeto) accede a su clase."], ["python-data-model", "python-tutorial"]),
  lesson("Medio", "python-medium-inheritance", "Herencia y super", "herencia", ["La herencia permite especializar comportamiento. super delega según el orden de resolución de métodos, no significa simplemente llamar al padre textual."], 'class Mensaje:\n    def texto(self): return "base"\nclass Alerta(Mensaje):\n    def texto(self): return super().texto().upper()', ["Diseña una relación de sustitución real.", "Prefiere composición para capacidades independientes."], tutorial),
  lesson("Medio", "python-medium-dataclasses", "Dataclasses", "dataclasses", ["@dataclass genera métodos como __init__ y __repr__ a partir de campos anotados. field controla valores predeterminados y fábricas."], 'from dataclasses import dataclass\n@dataclass(frozen=True)\nclass Punto:\n    x: int\n    y: int', ["default_factory evita compartir mutables.", "frozen limita asignaciones, pero no congela objetos anidados."], ["python-dataclasses", "python-library"], "python-medium-dataclasses-exercise"),
  lesson("Medio", "python-medium-property", "Propiedades", "property", ["property ofrece sintaxis de atributo con lógica de lectura o escritura. El almacenamiento debe usar otro nombre para evitar recursión."], 'class Temperatura:\n    def __init__(self, celsius): self._celsius = celsius\n    @property\n    def celsius(self): return self._celsius', ["Una propiedad mantiene una interfaz estable.", "No ocultes operaciones costosas tras una lectura sorprendente."], ["python-data-model", "python-tutorial"]),
  lesson("Medio", "python-medium-iterators", "Protocolo iterador", "iteradores", ["iter obtiene un iterador y next solicita valores hasta StopIteration. Un iterable puede crear recorridos independientes; un iterador conserva estado."], 'valores = iter([10, 20])\nprint(next(valores))', ["for gestiona StopIteration.", "Un iterador agotado no se reinicia."], ["python-iterators", "python-data-model"], "python-medium-iterators-exercise"),
  lesson("Medio", "python-medium-generators", "Funciones generadoras", "generadores", ["Una función con yield devuelve un generador. Cada next reanuda su marco hasta el siguiente yield, conservando variables locales."], 'def pares(limite):\n    for n in range(limite):\n        if n % 2 == 0:\n            yield n', ["Llamar la función no ejecuta todo el cuerpo.", "Los generadores producen datos bajo demanda."], ["python-iterators", "python-reference"], "python-medium-generators-exercise"),
  lesson("Medio", "python-medium-decorators", "Decoradores", "decoradores", ["Un decorador recibe el objeto definido y devuelve el objeto que quedará enlazado al nombre. functools.wraps conserva metadatos de la función envuelta."], 'from functools import wraps\ndef registrar(funcion):\n    @wraps(funcion)\n    def wrapper(*args, **kwargs):\n        return funcion(*args, **kwargs)\n    return wrapper', ["@decorador se aplica al definir.", "El wrapper debe respetar el contrato original."], ["python-functions", "python-functools"], "python-medium-decorators-exercise"),
  lesson("Medio", "python-medium-context-managers", "Context managers", "context-managers", ["with usa __enter__ y __exit__ para delimitar adquisición y liberación. La limpieza ocurre incluso si el bloque lanza una excepción."], 'class Recurso:\n    def __enter__(self): return self\n    def __exit__(self, exc_type, exc, tb): return False', ["Devolver True desde __exit__ suprime la excepción.", "La gestión del recurso queda junto a su uso."], ["python-contextlib", "python-data-model"]),
  lesson("Medio", "python-medium-safe-files", "Archivos y rutas seguras", "archivos", ["pathlib representa rutas y open debe usarse con with. Especificar encoding evita depender de la configuración local; las rutas recibidas deben validarse contra una carpeta permitida."], 'from pathlib import Path\nruta = Path("notas.txt")\nwith ruta.open("w", encoding="utf-8") as archivo:\n    archivo.write("hola")', ["No concatenes rutas no confiables sin validarlas.", "El sistema de archivos de Pyodide es virtual y efímero."], ["python-files", "python-library"]),
  lesson("Medio", "python-medium-json", "JSON", "json", ["json.dumps serializa datos compatibles y json.loads analiza texto JSON. JSON no preserva tuplas, claves no textuales ni objetos arbitrarios."], 'import json\ntexto = json.dumps({"activo": True})\ndato = json.loads(texto)', ["Analizar JSON no ejecuta código.", "Configura ensure_ascii según el contrato de salida."], ["python-json", "python-library"], "python-medium-json-exercise"),
  lesson("Medio", "python-medium-typing", "Anotaciones de tipo", "typing", ["Las anotaciones describen contratos para herramientas y lectores; Python no las impone automáticamente en runtime. Las uniones usan | y collections.abc contiene protocolos de uso común."], 'from collections.abc import Iterable\ndef total(valores: Iterable[int]) -> int:\n    return sum(valores)', ["Anota interfaces, no implementaciones innecesarias.", "None | str representa un valor opcional."], ["python-typing", "python-reference"]),
  lesson("Medio", "python-medium-advanced-exceptions", "Excepciones avanzadas y raise", "excepciones-avanzadas", ["raise crea o vuelve a propagar una excepción y raise ... from ... conserva una causa explícita. except puede manejar tipos concretos; else separa el camino correcto y finally garantiza limpieza, pero no debe ocultar un error con un return."], 'def cargar_entero(texto):\n    try:\n        return int(texto)\n    except ValueError as error:\n        raise ValueError("formato de entero inválido") from error\n    finally:\n        print("intento finalizado")', ["Usa raise sin argumentos dentro de except para conservar el traceback original.", "Encadena errores cuando traduzcas una excepción de bajo nivel al contrato de tu función."], ["python-tutorial", "python-reference"], "python-medium-advanced-exceptions-exercise"),
] as const;
