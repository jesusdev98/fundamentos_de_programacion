import { pythonExercise as exercise, pythonTest as test } from "../helpers.ts";

const model = ["python-data-model", "python-reference"] as const;

export const difficultExercises = [
  exercise(1, "Difícil", "python-difficult-dunder-exercise", "Iterable con recorridos independientes", "iteradores-personalizados", "python-difficult-dunder", "Implementa Coleccion como iterable reiniciable y un IteradorColeccion separado que conserve su posición, devuelva cada valor y termine con StopIteration.", "class Coleccion:\n    def __init__(self, valores):\n        self._valores = tuple(valores)\n    def __iter__(self):\n        pass\n\nclass IteradorColeccion:\n    def __init__(self, valores):\n        pass\n    def __iter__(self):\n        pass\n    def __next__(self):\n        pass", ["Coleccion.__iter__ crea un IteradorColeccion nuevo.", "El iterador devuelve self y avanza un índice hasta lanzar StopIteration."], "class Coleccion:\n    def __init__(self, valores):\n        self._valores = tuple(valores)\n    def __iter__(self):\n        return IteradorColeccion(self._valores)\n\nclass IteradorColeccion:\n    def __init__(self, valores):\n        self._valores = valores\n        self._posicion = 0\n    def __iter__(self):\n        return self\n    def __next__(self):\n        if self._posicion >= len(self._valores):\n            raise StopIteration\n        valor = self._valores[self._posicion]\n        self._posicion += 1\n        return valor", "Separar ambos protocolos permite crear varios recorridos sobre la misma colección sin compartir posición.", { kind: "tests", tests: [test("iterate", "Recorre todos los valores", "list(Coleccion([1, 2, 3]))", [1, 2, 3], "deepEqual"), test("independent", "Crea recorridos independientes", "(lambda c: (lambda a, b: [next(a), next(a), next(b)])(iter(c), iter(c)))(Coleccion([1, 2]))", [1, 2, 1], "deepEqual"), test("exhaustion", "Termina con StopIteration", "next(iter(Coleccion([])))", "StopIteration", "raises")] }, ["python-iterators", "python-data-model"]),
  exercise(2, "Difícil", "python-difficult-descriptor-exercise", "Descriptor de rango", "descriptors", "python-difficult-descriptors", "Implementa Rango(minimo, maximo) con __set_name__, __get__ y __set__ para validar valores inclusivos.", "class Rango:\n    def __init__(self, minimo, maximo):\n        self.minimo = minimo\n        self.maximo = maximo\n    # Completa el descriptor", ["Guarda un nombre privado en __set_name__.", "__get__ debe devolver self cuando obj sea None."], 'class Rango:\n    def __init__(self, minimo, maximo):\n        self.minimo = minimo\n        self.maximo = maximo\n    def __set_name__(self, owner, name):\n        self.nombre = "_" + name\n    def __get__(self, obj, owner=None):\n        return self if obj is None else getattr(obj, self.nombre)\n    def __set__(self, obj, valor):\n        if not self.minimo <= valor <= self.maximo:\n            raise ValueError("fuera de rango")\n        setattr(obj, self.nombre, valor)\nclass Nivel:\n    valor = Rango(0, 100)\n    def __init__(self, valor): self.valor = valor', "El descriptor de datos intercepta todas las asignaciones a valor.", { kind: "tests", tests: [test("valid", "Conserva un valor válido", "Nivel(42).valor", 42), test("descriptor", "Se accede desde la clase", "isinstance(Nivel.valor, Rango)", true), test("reject", "Rechaza fuera de rango", "Nivel(101)", "ValueError", "raises")] }, ["python-descriptors", "python-data-model"]),
  exercise(3, "Difícil", "python-difficult-mro-exercise", "Cadena cooperativa", "mro", "python-difficult-multiple-inheritance", "Completa Auditoria y Cache para añadir sus etiquetas y delegar con super hasta Base.procesar.", "class Base:\n    def procesar(self, dato): return [dato]\nclass Auditoria(Base):\n    def procesar(self, dato):\n        pass\nclass Cache(Base):\n    def procesar(self, dato):\n        pass\nclass Servicio(Auditoria, Cache):\n    pass", ["Cada método devuelve su etiqueta seguida del resultado de super().", "El MRO de Servicio pasa por Auditoria y Cache."], 'class Base:\n    def procesar(self, dato): return [dato]\nclass Auditoria(Base):\n    def procesar(self, dato):\n        return ["auditoria", *super().procesar(dato)]\nclass Cache(Base):\n    def procesar(self, dato):\n        return ["cache", *super().procesar(dato)]\nclass Servicio(Auditoria, Cache):\n    pass', "super sigue Servicio, Auditoria, Cache y Base de forma cooperativa.", { kind: "tests", tests: [test("chain", "Respeta toda la cadena", "Servicio().procesar('dato')", ["auditoria", "cache", "dato"], "deepEqual"), test("mro", "Conserva el MRO", "[clase.__name__ for clase in Servicio.mro()[:4]]", ["Servicio", "Auditoria", "Cache", "Base"], "deepEqual")] }, ["python-mro", "python-data-model"]),
  exercise(4, "Difícil", "python-difficult-protocol-exercise", "Repositorio estructural", "protocol", "python-difficult-protocol", "Declara Buscable como Protocol con buscar(clave) -> str | None e implementa Memoria compatible sin heredar del protocolo.", "from typing import Protocol\n# Declara Buscable y Memoria", ["El método del protocolo usa ... como cuerpo.", "Memoria sólo necesita la misma firma observable."], "from typing import Protocol\nclass Buscable(Protocol):\n    def buscar(self, clave: str) -> str | None: ...\nclass Memoria:\n    def __init__(self, datos): self.datos = datos\n    def buscar(self, clave: str) -> str | None:\n        return self.datos.get(clave)", "La compatibilidad estructural no exige herencia nominal.", { kind: "tests", tests: [test("found", "Encuentra una clave", "Memoria({'a': 'uno'}).buscar('a')", "uno"), test("missing", "Devuelve None si falta", "Memoria({}).buscar('a') is None", true)] }, ["python-typing", "python-reference"]),
  exercise(5, "Difícil", "python-difficult-generics-exercise", "Pila genérica", "generics", "python-difficult-generics", "Implementa Pila[T] con push(valor: T), pop() -> T y len mediante __len__.", "class Pila[T]:\n    def __init__(self):\n        pass\n    # Completa", ["Almacena list[T] en self._datos.", "pop delega en la lista."], "class Pila[T]:\n    def __init__(self):\n        self._datos: list[T] = []\n    def push(self, valor: T) -> None:\n        self._datos.append(valor)\n    def pop(self) -> T:\n        return self._datos.pop()\n    def __len__(self) -> int:\n        return len(self._datos)", "El parámetro T conecta los valores aceptados con el retorno de pop.", { kind: "tests", tests: [test("lifo", "Respeta LIFO", "(lambda p: (p.push(1), p.push(2), p.pop())[2])(Pila[int]())", 2), test("length", "Expone la longitud", "(lambda p: (p.push('x'), len(p))[1])(Pila[str]())", 1)] }, ["python-typing", "python-reference"]),
  exercise(6, "Difícil", "python-difficult-closures-exercise", "Captura estable en bucles", "closures", "python-difficult-closures", "Implementa crear_multiplicadores(factores) para devolver funciones que conserven cada factor, sin late binding.", "def crear_multiplicadores(factores):\n    funciones = []\n    # Añade un closure por factor\n    return funciones", ["Crea el closure mediante otra llamada que reciba factor.", "Cada llamada exterior crea una celda independiente."], "def crear_multiplicadores(factores):\n    funciones = []\n    def crear(factor):\n        return lambda valor: valor * factor\n    for factor in factores:\n        funciones.append(crear(factor))\n    return funciones", "La fábrica interior fija cada factor en un entorno distinto.", { kind: "tests", tests: [test("values", "Conserva cada factor", "[f(10) for f in crear_multiplicadores([2, 3, 4])]", [20, 30, 40], "deepEqual"), test("reuse", "Permite reutilizar un closure", "crear_multiplicadores([5])[0](3)", 15)] }, ["python-functions", "python-reference"]),
  exercise(7, "Difícil", "python-difficult-decorators-exercise", "Reintentos parametrizados", "decorators", "python-difficult-parameterized-decorators", "Implementa reintentar(intentos) para repetir una función que lance ValueError y propagar el último fallo al agotar intentos.", "from functools import wraps\ndef reintentar(intentos):\n    pass", ["La llamada exterior devuelve decorar.", "Dentro del wrapper, captura ValueError y usa raise en el último intento."], "from functools import wraps\ndef reintentar(intentos):\n    def decorar(funcion):\n        @wraps(funcion)\n        def wrapper(*args, **kwargs):\n            for indice in range(intentos):\n                try:\n                    return funcion(*args, **kwargs)\n                except ValueError:\n                    if indice == intentos - 1:\n                        raise\n        return wrapper\n    return decorar", "La configuración intentos queda capturada y cada llamada mantiene su propio flujo.", { kind: "tests", tests: [test("eventual", "Devuelve tras recuperarse", "(lambda estado: reintentar(3)(lambda: estado.append(1) or (len(estado) >= 2 and 'ok') or (_ for _ in ()).throw(ValueError()))())([])", "ok"), test("metadata", "Conserva el nombre", "reintentar(2)(lambda: None).__name__", "<lambda>")] }, ["python-functools", "python-functions"]),
  exercise(8, "Difícil", "python-difficult-generator-control-exercise", "Acumulador controlable", "generator-control", "python-difficult-generator-control", "Implementa acumulador(estado): produce el total, suma valores recibidos con send, reinicia a cero si throw inyecta ValueError y marca estado['cerrado'] = True al cerrarse.", "def acumulador(estado):\n    # Gestiona send, throw y close\n    pass", ["Captura ValueError alrededor de la expresión yield para reiniciar.", "Usa finally para marcar el cierre, también cuando close provoque GeneratorExit."], 'def acumulador(estado):\n    total = 0\n    try:\n        while True:\n            try:\n                valor = yield total\n            except ValueError:\n                total = 0\n            else:\n                if valor is not None:\n                    total += valor\n    finally:\n        estado["cerrado"] = True', "send comunica datos con el generador, throw permite manejar un fallo en el punto suspendido y close garantiza la limpieza de finally.", { kind: "tests", tests: [test("send", "Acumula valores enviados", "(lambda g: (next(g), g.send(3), g.send(4))[2])(acumulador({}))", 7), test("throw", "Reinicia tras ValueError", "(lambda g: (next(g), g.send(5), g.throw(ValueError), g.send(2))[3])(acumulador({}))", 2), test("close", "Ejecuta la limpieza al cerrar", "(lambda estado: (lambda g: (next(g), g.close(), estado['cerrado'])[2])(acumulador(estado)))({})", true)] }, ["python-iterators", "python-reference"]),
  exercise(9, "Difícil", "python-difficult-async-exercise", "Coordinar coroutines", "async-await", "python-difficult-async-await", "Implementa ejecutar() para iniciar valor(2) y valor(4) como tareas, esperar ambas y devolver su suma.", "import asyncio\nasync def valor(numero):\n    await asyncio.sleep(0)\n    return numero\nasync def ejecutar():\n    pass\n# Guarda resultado con asyncio.run", ["Usa asyncio.create_task para iniciar ambas.", "Espera las dos tareas antes de sumar."], "import asyncio\nasync def valor(numero):\n    await asyncio.sleep(0)\n    return numero\nasync def ejecutar():\n    una = asyncio.create_task(valor(2))\n    otra = asyncio.create_task(valor(4))\n    return await una + await otra\nresultado = asyncio.run(ejecutar())", "Las tareas progresan de forma concurrente cuando cada coroutine cede control.", { kind: "tests", tests: [test("result", "Coordina ambos resultados", "resultado", 6)] }, ["python-asyncio", "python-reference"]),
  exercise(10, "Difícil", "python-difficult-taskgroup-exercise", "TaskGroup estructurado", "taskgroup", "python-difficult-taskgroup", "Implementa ejecutar() con TaskGroup para crear tres tareas valor(1..3) y devolver sus resultados en orden.", "import asyncio\nasync def valor(numero):\n    await asyncio.sleep(0)\n    return numero\nasync def ejecutar():\n    pass", ["Guarda las Tasks creadas por grupo.create_task.", "Lee result() después de salir del async with."], "import asyncio\nasync def valor(numero):\n    await asyncio.sleep(0)\n    return numero\nasync def ejecutar():\n    async with asyncio.TaskGroup() as grupo:\n        tareas = [grupo.create_task(valor(n)) for n in range(1, 4)]\n    return [tarea.result() for tarea in tareas]\nresultados = asyncio.run(ejecutar())", "El bloque espera todas las tareas antes de leer resultados.", { kind: "tests", tests: [test("results", "Reúne los resultados", "resultados", [1, 2, 3], "deepEqual")] }, ["python-asyncio", "python-library"]),
  exercise(11, "Difícil", "python-difficult-async-iterator-exercise", "Generador asíncrono", "async-iterators", "python-difficult-async-iterators", "Implementa contar(limite) como generador asíncrono que produzca 0..limite-1 y recopílalo con async for.", "import asyncio\nasync def contar(limite):\n    pass\nasync def recopilar():\n    valores = []\n    # Usa async for\n    return valores", ["Una función async con yield es un generador asíncrono.", "Añade cada valor dentro de async for."], "import asyncio\nasync def contar(limite):\n    for numero in range(limite):\n        await asyncio.sleep(0)\n        yield numero\nasync def recopilar():\n    valores = []\n    async for valor in contar(3):\n        valores.append(valor)\n    return valores\nresultados = asyncio.run(recopilar())", "async for espera cada __anext__ sin bloquear activamente el event loop.", { kind: "tests", tests: [test("values", "Recopila en orden", "resultados", [0, 1, 2], "deepEqual")] }, ["python-asyncio", "python-data-model"]),
  exercise(12, "Difícil", "python-difficult-hash-exercise", "Clave de valor", "hash", "python-difficult-hash", "Implementa Clave inmutable con id y region, igualdad por ambos campos y hash coherente.", "class Clave:\n    def __init__(self, id, region):\n        pass\n    # Implementa igualdad y hash", ["Guarda los campos sin ofrecer mutadores.", "Calcula hash de la tupla (id, region)."], "class Clave:\n    def __init__(self, id, region):\n        self.id = id\n        self.region = region\n    def __eq__(self, otro):\n        if not isinstance(otro, Clave):\n            return NotImplemented\n        return (self.id, self.region) == (otro.id, otro.region)\n    def __hash__(self):\n        return hash((self.id, self.region))", "Igualdad y hash usan exactamente el mismo estado de valor.", { kind: "tests", tests: [test("equal", "Compara por valor", "Clave(1, 'es') == Clave(1, 'es')", true), test("dict", "Funciona como clave", "{Clave(1, 'es'): 'ok'}[Clave(1, 'es')]", "ok"), test("different", "Distingue regiones", "Clave(1, 'es') != Clave(1, 'fr')", true)] }, model),
  exercise(13, "Difícil", "python-difficult-pattern-exercise", "Desestructurar eventos", "pattern-matching", "python-difficult-pattern-matching", "Implementa describir(evento) para reconocer {'tipo':'click','x':x,'y':y}, ['error', mensaje] y un caso desconocido.", "def describir(evento):\n    match evento:\n        # Completa los casos\n        pass", ["Los patrones mapping pueden omitir claves adicionales.", "Usa una secuencia literal para el error."], 'def describir(evento):\n    match evento:\n        case {"tipo": "click", "x": x, "y": y}:\n            return f"click {x},{y}"\n        case ["error", mensaje]:\n            return f"error: {mensaje}"\n        case _:\n            return "desconocido"', "Los patrones comprueban estructura y capturan sólo los datos necesarios.", { kind: "tests", tests: [test("mapping", "Reconoce el mapping", "describir({'tipo': 'click', 'x': 2, 'y': 3})", "click 2,3"), test("sequence", "Reconoce la secuencia", "describir(['error', 'red'])", "error: red"), test("fallback", "Cubre otros casos", "describir(None)", "desconocido")] }, ["python-pattern-matching", "python-reference"]),
  exercise(14, "Difícil", "python-difficult-cache-exercise", "Fibonacci con caché", "caching", "python-difficult-caching", "Decora fibonacci con lru_cache(maxsize=64) e implementa la recurrencia; calcula fibonacci(10) en resultado.", "from functools import lru_cache\n# Decora e implementa fibonacci", ["Los casos 0 y 1 devuelven n.", "La recurrencia suma los dos valores anteriores."], "from functools import lru_cache\n@lru_cache(maxsize=64)\ndef fibonacci(n):\n    if n < 2:\n        return n\n    return fibonacci(n - 1) + fibonacci(n - 2)\nresultado = fibonacci(10)", "La caché evita recalcular el mismo subproblema recursivo.", { kind: "tests", tests: [test("value", "Calcula fibonacci(10)", "resultado", 55), test("cache", "Registra aciertos", "fibonacci.cache_info().hits > 0", true)] }, ["python-functools", "python-library"]),
] as const;
