import { pythonLesson as lesson } from "../helpers.ts";

const model = ["python-data-model", "python-reference"] as const;
export const difficultLessons = [
  lesson("Difícil", "python-difficult-data-model", "Modelo de datos", "data-model", ["El modelo de datos conecta sintaxis y operaciones con métodos especiales. type, identidad, valor y mutabilidad determinan cómo participa un objeto en el lenguaje."], 'class Caja:\n    def __init__(self, valor): self.valor = valor\n    def __repr__(self): return f"Caja({self.valor!r})"', ["La sintaxis delega en protocolos documentados.", "No llames métodos especiales de forma directa salvo que el protocolo lo requiera."], model),
  lesson("Difícil", "python-difficult-dunder", "Iteradores personalizados avanzados", "iteradores-personalizados", ["Un iterable personalizado implementa __iter__ y puede crear un iterador independiente con __next__; este conserva su estado y termina con StopIteration. Separar iterable e iterador permite recorridos simultáneos y reiniciables, mientras otros métodos como __len__ y __contains__ completan un protocolo coherente."], 'class CuentaAtras:\n    def __init__(self, inicio): self.inicio = inicio\n    def __iter__(self): return IteradorCuentaAtras(self.inicio)\nclass IteradorCuentaAtras:\n    def __init__(self, actual): self.actual = actual\n    def __iter__(self): return self\n    def __next__(self):\n        if self.actual == 0: raise StopIteration\n        self.actual -= 1\n        return self.actual + 1', ["__iter__ del iterable crea estado independiente; __iter__ del iterador devuelve self.", "StopIteration es parte del protocolo y no un error que deba ocultarse."], ["python-iterators", "python-data-model"], "python-difficult-dunder-exercise"),
  lesson("Difícil", "python-difficult-descriptors", "Descriptores", "descriptors", ["Un descriptor define __get__, __set__ o __delete__ y controla acceso a atributos desde la clase propietaria. Un descriptor de datos tiene prioridad sobre el diccionario de instancia."], 'class Positivo:\n    def __set_name__(self, owner, name): self.name = "_" + name\n    def __get__(self, obj, owner=None): return getattr(obj, self.name)\n    def __set__(self, obj, value):\n        if value <= 0: raise ValueError("debe ser positivo")\n        setattr(obj, self.name, value)', ["__set_name__ recibe el nombre declarado.", "property, classmethod y funciones usan el protocolo descriptor."], ["python-descriptors", "python-data-model"], "python-difficult-descriptor-exercise"),
  lesson("Difícil", "python-difficult-mro", "MRO y C3", "mro", ["El MRO linealiza una jerarquía respetando precedencia local y monotonía. Clase.mro() permite inspeccionar el orden usado para resolver atributos y super."], 'class A: pass\nclass B(A): pass\nclass C(A): pass\nclass D(B, C): pass\nprint(D.mro())', ["El orden de bases forma parte del contrato.", "Una jerarquía inconsistente se rechaza al crear la clase."], ["python-mro", "python-data-model"]),
  lesson("Difícil", "python-difficult-multiple-inheritance", "Herencia múltiple cooperativa", "multiple-inheritance", ["En una jerarquía cooperativa cada método consume sus argumentos y llama super para continuar por el MRO. Saltarse la cadena rompe combinaciones posteriores."], 'class Registro:\n    def guardar(self, dato): return [dato]\nclass Auditoria(Registro):\n    def guardar(self, dato): return ["audit", *super().guardar(dato)]', ["super no nombra una clase padre fija.", "Las firmas compatibles facilitan cooperación."], ["python-mro", "python-data-model"], "python-difficult-mro-exercise"),
  lesson("Difícil", "python-difficult-abc", "Clases base abstractas", "abc", ["ABC y @abstractmethod declaran operaciones obligatorias y evitan instanciar clases incompletas. También pueden proporcionar una implementación reutilizable."], 'from abc import ABC, abstractmethod\nclass Repositorio(ABC):\n    @abstractmethod\n    def obtener(self, id): ...', ["Una ABC usa herencia nominal.", "Registrar una subclase virtual no copia métodos."], ["python-abc", "python-library"]),
  lesson("Difícil", "python-difficult-protocol", "Protocol y tipado estructural", "protocol", ["typing.Protocol describe una interfaz por estructura para análisis estático. Una clase compatible no necesita heredar del protocolo."], 'from typing import Protocol\nclass Cerrable(Protocol):\n    def close(self) -> None: ...\ndef cerrar(recurso: Cerrable) -> None:\n    recurso.close()', ["Protocol reduce acoplamiento nominal.", "@runtime_checkable sólo admite comprobaciones estructurales limitadas."], ["python-typing", "python-reference"], "python-difficult-protocol-exercise"),
  lesson("Difícil", "python-difficult-generics", "Genéricos", "generics", ["Los parámetros de tipo expresan relaciones entre valores. Los límites y restricciones documentan qué operaciones puede asumir una implementación."], 'class Pila[T]:\n    def __init__(self): self._datos: list[T] = []\n    def push(self, valor: T) -> None: self._datos.append(valor)\n    def pop(self) -> T: return self._datos.pop()', ["La varianza depende de cómo se usa el parámetro.", "El tipado se borra en gran medida durante runtime."], ["python-typing", "python-reference"], "python-difficult-generics-exercise"),
  lesson("Difícil", "python-difficult-closures", "Closures y celdas", "closures", ["Una función interna conserva celdas del ámbito envolvente. nonlocal reasigna la celda; capturar una variable de bucle sin fijarla produce late binding."], 'def multiplicador(factor):\n    def aplicar(valor):\n        return valor * factor\n    return aplicar', ["El closure conserva referencias, no fotografías.", "Un argumento predeterminado puede fijar un valor por iteración."], ["python-functions", "python-reference"], "python-difficult-closures-exercise"),
  lesson("Difícil", "python-difficult-parameterized-decorators", "Decoradores parametrizados", "decorators", ["Un decorador parametrizado añade una fábrica: la llamada exterior recibe configuración, devuelve el decorador y este devuelve el wrapper."], 'from functools import wraps\ndef repetir(veces):\n    def decorar(funcion):\n        @wraps(funcion)\n        def wrapper(*args, **kwargs):\n            return [funcion(*args, **kwargs) for _ in range(veces)]\n        return wrapper\n    return decorar', ["La configuración queda en un closure.", "wraps conserva __wrapped__ y metadatos."], ["python-functools", "python-functions"], "python-difficult-decorators-exercise"),
  lesson("Difícil", "python-difficult-generator-control", "send, throw y close", "generator-control", ["send entrega un valor al yield suspendido, throw inyecta una excepción y close provoca GeneratorExit. El primer avance debe usar next o send(None)."], 'def acumulador():\n    total = 0\n    while True:\n        valor = yield total\n        if valor is not None:\n            total += valor', ["Un finally permite limpiar al cerrar.", "No captures GeneratorExit para seguir produciendo valores."], ["python-iterators", "python-reference"], "python-difficult-generator-control-exercise"),
  lesson("Difícil", "python-difficult-contextlib", "Context managers personalizados y contextlib", "context-managers-personalizados", ["Un context manager personalizado implementa __enter__ y __exit__ para adquirir, liberar y decidir si propaga una excepción. @contextmanager expresa el mismo contrato con un generador de un único yield, y ExitStack compone dinámicamente varios recursos."], 'class Recurso:\n    def __enter__(self):\n        print("abrir")\n        return self\n    def __exit__(self, exc_type, exc, traceback):\n        print("cerrar")\n        return False', ["Devolver False desde __exit__ conserva cualquier excepción del bloque.", "@contextmanager necesita exactamente un yield efectivo y limpieza en finally."], ["python-contextlib", "python-data-model"]),
  lesson("Difícil", "python-difficult-async-await", "Coroutines y async/await", "async-await", ["async def crea funciones de coroutine. await suspende la coroutine actual hasta completar un awaitable sin bloquear activamente el hilo del event loop."], 'import asyncio\nasync def calcular():\n    await asyncio.sleep(0)\n    return 7\nprint(asyncio.run(calcular()))', ["Llamar async def produce un objeto coroutine.", "El trabajo de CPU sigue bloqueando el hilo si no cede control."], ["python-asyncio", "python-reference"], "python-difficult-async-exercise"),
  lesson("Difícil", "python-difficult-event-loop", "Event loop y Tasks", "event-loop", ["El event loop agenda callbacks y avanza Tasks cuando sus coroutines pueden continuar. create_task mantiene una referencia a una ejecución concurrente cooperativa."], 'import asyncio\nasync def paso(nombre):\n    await asyncio.sleep(0)\n    return nombre\nasync def main():\n    tarea = asyncio.create_task(paso("A"))\n    return await tarea', ["Concurrencia no implica paralelismo de CPU.", "Conserva referencias a Tasks en segundo plano."], ["python-asyncio", "python-library"]),
  lesson("Difícil", "python-difficult-taskgroup", "Concurrencia estructurada con TaskGroup", "taskgroup", ["asyncio.TaskGroup espera todas las tareas al salir. Si una falla, cancela las restantes y agrupa errores no relacionados con cancelación."], 'import asyncio\nasync def main():\n    async with asyncio.TaskGroup() as grupo:\n        uno = grupo.create_task(asyncio.sleep(0, result=1))\n        dos = grupo.create_task(asyncio.sleep(0, result=2))\n    return uno.result() + dos.result()', ["Las tareas no escapan del bloque sin completar.", "Los fallos pueden llegar como ExceptionGroup."], ["python-asyncio", "python-library"], "python-difficult-taskgroup-exercise"),
  lesson("Difícil", "python-difficult-cancellation", "Errores asíncronos y cancelación", "cancellation", ["Una excepción de una Task se conserva hasta observar su resultado o esperarla. Task.cancel solicita cancelación inyectando CancelledError en el siguiente punto de suspensión; la limpieza debe usar finally y normalmente volver a propagar la cancelación."], 'import asyncio\nasync def trabajo():\n    try:\n        await asyncio.sleep(10)\n    except asyncio.CancelledError:\n        print("cancelada")\n        raise\n    finally:\n        print("limpieza")', ["CancelledError hereda directamente de BaseException.", "Observa los errores de las Tasks y no suprimas cancelación salvo que restaures deliberadamente el contrato."], ["python-asyncio", "python-library"]),
  lesson("Difícil", "python-difficult-async-iterators", "Iteradores asíncronos", "async-iterators", ["async for usa __aiter__ y __anext__; este devuelve un awaitable y termina con StopAsyncIteration. Un generador asíncrono combina async def con yield."], 'import asyncio\nasync def eventos():\n    for valor in range(3):\n        await asyncio.sleep(0)\n        yield valor', ["async for puede ceder entre elementos.", "aclose permite finalizar un generador asíncrono."], ["python-asyncio", "python-data-model"], "python-difficult-async-iterator-exercise"),
  lesson("Difícil", "python-difficult-async-context", "Context managers asíncronos", "async-context", ["async with espera __aenter__ y __aexit__. @asynccontextmanager permite expresar adquisición y limpieza asíncronas con un generador."], 'from contextlib import asynccontextmanager\n@asynccontextmanager\nasync def sesion():\n    try:\n        yield "activa"\n    finally:\n        pass', ["La limpieza puede contener await.", "aclosing asegura aclose al abandonar temprano."], ["python-contextlib", "python-data-model"]),
  lesson("Difícil", "python-difficult-copy", "Copia superficial y profunda", "copy", ["copy.copy crea otro contenedor y comparte referencias internas. copy.deepcopy recorre el grafo con memo para preservar ciclos y evitar duplicar el mismo objeto varias veces."], 'from copy import copy, deepcopy\noriginal = {"config": {"activo": True}}\nsuperficial = copy(original)\nprofunda = deepcopy(original)', ["Una copia profunda no siempre es deseable.", "Las clases pueden personalizar __copy__ y __deepcopy__."], ["python-copy", "python-library"]),
  lesson("Difícil", "python-difficult-hash", "Igualdad y hash", "hash", ["Los objetos hashables deben mantener un hash estable y los objetos iguales deben compartir hash. Sobrescribir __eq__ suele desactivar __hash__ para evitar claves mutables incoherentes."], 'from dataclasses import dataclass\n@dataclass(frozen=True)\nclass Clave:\n    id: int\nprint(hash(Clave(3)))', ["La inmutabilidad práctica favorece hash estable.", "Las colisiones son válidas; la igualdad las distingue."], model, "python-difficult-hash-exercise"),
  lesson("Difícil", "python-difficult-imports", "Sistema de importación", "imports", ["import consulta sys.modules y usa finders y loaders descritos por ModuleSpec. Los imports relativos dependen del paquete, no de la carpeta de trabajo."], 'import importlib\nmodulo = importlib.import_module("json")\nprint(modulo.__spec__.name)', ["sys.modules es la caché de módulos.", "Modificar sys.path globalmente vuelve frágil la resolución."], ["python-imports", "python-reference"]),
  lesson("Difícil", "python-difficult-pattern-matching", "Pattern matching estructural", "pattern-matching", ["Los patrones pueden desestructurar secuencias, mappings y clases. Las guardas se evalúan después de que el patrón capture nombres; el orden de casos es significativo."], 'def clasificar(valor):\n    match valor:\n        case {"tipo": "punto", "x": x, "y": y} if x == y:\n            return "diagonal"\n        case [primero, *resto]:\n            return primero, resto\n        case _:\n            return None', ["Un nombre simple captura; no compara una constante.", "__match_args__ define posiciones de patrones de clase."], ["python-pattern-matching", "python-reference"], "python-difficult-pattern-exercise"),
  lesson("Difícil", "python-difficult-functools", "partial y singledispatch", "functools", ["functools.partial fija argumentos sin ejecutar todavía. singledispatch selecciona una implementación por el tipo del primer argumento en runtime."], 'from functools import partial, singledispatch\ndoble = partial(pow, exp=2)\n@singledispatch\ndef describir(valor): return "objeto"\n@describir.register\ndef _(valor: int): return "entero"', ["singledispatch no implementa despacho múltiple.", "partial conserva atributos func, args y keywords."], ["python-functools", "python-library"]),
  lesson("Difícil", "python-difficult-caching", "Cachés con functools", "caching", ["@cache memoriza por argumentos hashables sin límite; @lru_cache puede acotar entradas. Sólo es correcto reutilizar resultados cuando la función es estable para esos argumentos."], 'from functools import lru_cache\n@lru_cache(maxsize=128)\ndef fibonacci(n):\n    return n if n < 2 else fibonacci(n - 1) + fibonacci(n - 2)', ["La caché mantiene referencias a argumentos y resultados.", "cache_info permite observar aciertos, fallos y tamaño."], ["python-functools", "python-library"], "python-difficult-cache-exercise"),
] as const;
