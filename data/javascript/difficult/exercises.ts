import type { Exercise, ExerciseTest } from "@/types/learning";

const test = (id: string, label: string, expression: string, expected: unknown, assertion: ExerciseTest["assertion"] = "equal"): ExerciseTest => ({ id, label, expression, assertion, expected });
const exercise = (number: number, id: string, title: string, concept: string, lessonId: string, prompt: string, starterCode: string, hints: readonly string[], solution: string, explanation: string, tests: readonly ExerciseTest[], sourceIds: readonly string[]): Exercise => ({ number, id, title, concept, lessonId, prompt, starterCode, hints, solution, explanation, validation: { kind: "tests", tests }, difficulty: "Difícil", sourceIds });

export const difficultExercises: readonly Exercise[] = [
  exercise(1, "difficult-closure-counter", "Contador mediante closure", "closures", "difficult-closures", "Implementa crearContador(inicial). Debe devolver una función que incremente y devuelva su binding privado en cada llamada.", "function crearContador(inicial) {\n  // Devuelve el closure\n}\nconst contar = crearContador(2);", ["Declara el estado en la función exterior.", "La función interior debe actualizar el mismo binding con += 1."], "function crearContador(inicial) {\n  let valor = inicial;\n  return function () {\n    valor += 1;\n    return valor;\n  };\n}\nconst contar = crearContador(2);", "El closure conserva acceso al binding valor. No guarda una copia: cada llamada actualiza y observa el mismo binding de esa instancia.", [test("counter-sequence", "Mantiene una secuencia", "[contar(), contar(), contar()]", [3, 4, 5], "deepEqual"), test("counter-new", "Acepta otro valor inicial", "crearContador(9)()", 10)], ["mdn-closures", "ecma-262"]),
  exercise(2, "difficult-private-closure-state", "Estados privados independientes", "closures", "difficult-closures", "Implementa crearCuenta(saldoInicial) con depositar y consultar. Dos cuentas deben mantener bindings privados independientes.", "function crearCuenta(saldoInicial) {\n  // Devuelve la API pública\n}\nconst cajaA = crearCuenta(10);\nconst cajaB = crearCuenta(3);", ["Crea saldo dentro de cada llamada a crearCuenta.", "Devuelve métodos que cierren sobre saldo, sin exponerlo como propiedad."], "function crearCuenta(saldoInicial) {\n  let saldo = saldoInicial;\n  return {\n    depositar(monto) { saldo += monto; return saldo; },\n    consultar() { return saldo; },\n  };\n}\nconst cajaA = crearCuenta(10);\nconst cajaB = crearCuenta(3);", "Cada invocación crea un entorno nuevo. Los métodos de una cuenta comparten su binding saldo, pero no el binding de otra cuenta.", [test("private-a", "Actualiza la primera cuenta", "cajaA.depositar(5)", 15), test("private-b", "No mezcla la segunda cuenta", "cajaB.consultar()", 3), test("not-property", "No expone saldo como propiedad", "Object.hasOwn(cajaA, 'saldo')", false)], ["mdn-closures", "ecma-262"]),
  exercise(3, "difficult-this-call-site", "Resolver this por call-site", "this", "difficult-this", "Define describir como función normal. Reutilízala como método de norte y sur para que lea el nombre del receptor en cada call-site.", "function describir() {\n  // Devuelve this.nombre\n}\nconst norte = { nombre: \"Norte\", describir };\nconst sur = { nombre: \"Sur\", describir };", ["No conviertas describir en arrow.", "La llamada norte.describir() establece norte como receptor."], "function describir() {\n  return this.nombre;\n}\nconst norte = { nombre: \"Norte\", describir };\nconst sur = { nombre: \"Sur\", describir };", "La misma función produce resultados distintos porque una función normal obtiene this de cada forma de invocación.", [test("this-north", "Lee el receptor Norte", "norte.describir()", "Norte"), test("this-south", "Lee el receptor Sur", "sur.describir()", "Sur"), test("this-call", "Permite un receptor explícito", "describir.call({ nombre: 'Este' })", "Este")], ["mdn-this", "ecma-262"]),
  exercise(4, "difficult-bind-context", "Fijar contexto con bind", "call-apply-bind", "difficult-call-apply-bind", "Crea presentarSaludo enlazando presentar a persona y prefijando 'Hola'. Debe aceptar todavía el signo de cierre.", "function presentar(inicio, cierre) {\n  return `${inicio}, ${this.nombre}${cierre}`;\n}\nconst persona = { nombre: \"Lina\" };\n// Declara presentarSaludo", ["bind devuelve una función nueva.", "Pasa persona como primer argumento de bind y Hola como argumento parcial."], "function presentar(inicio, cierre) {\n  return `${inicio}, ${this.nombre}${cierre}`;\n}\nconst persona = { nombre: \"Lina\" };\nconst presentarSaludo = presentar.bind(persona, \"Hola\");", "bind fija el receptor y el primer argumento sin ejecutar todavía presentar; el cierre se recibe en cada llamada posterior.", [test("bind-greeting", "Conserva contexto y argumento parcial", "presentarSaludo('!')", "Hola, Lina!"), test("bind-reuse", "Reutiliza la función enlazada", "presentarSaludo('.')", "Hola, Lina.")], ["mdn-this", "ecma-262"]),
  exercise(5, "difficult-prototype-method", "Delegar un método al prototype", "prototype-chain", "difficult-prototype-chain", "Crea reporte con Object.create(base), agrega una propiedad propia valor con 6 y usa el método heredado duplicar.", "const base = {\n  duplicar() { return this.valor * 2; },\n};\n// Declara reporte", ["Object.create recibe el prototipo deseado.", "Asigna valor en reporte, no en base."], "const base = {\n  duplicar() { return this.valor * 2; },\n};\nconst reporte = Object.create(base);\nreporte.valor = 6;", "reporte no posee duplicar: la lectura lo encuentra en base y la llamada conserva reporte como this.", [test("prototype-result", "Ejecuta el método heredado", "reporte.duplicar()", 12), test("prototype-link", "Conserva el enlace prototípico", "Object.getPrototypeOf(reporte) === base", true), test("prototype-own", "No copia el método", "Object.hasOwn(reporte, 'duplicar')", false)], ["mdn-prototype-chain", "ecma-262"]),
  exercise(6, "difficult-class-inheritance", "Clase derivada con super", "class-inheritance", "difficult-class-inheritance", "Implementa Rectangulo con area(). Extiende Cuadrado, llama super con el mismo lado para ancho y alto y crea figura de lado 5.", "class Rectangulo {\n  constructor(ancho, alto) {\n    this.ancho = ancho;\n    this.alto = alto;\n  }\n  area() { return this.ancho * this.alto; }\n}\nclass Cuadrado extends Rectangulo {\n  constructor(lado) {\n    // Completa\n  }\n}\n// Declara figura", ["El constructor derivado debe llamar super antes de usar this.", "Entrega lado dos veces al constructor base."], "class Rectangulo {\n  constructor(ancho, alto) {\n    this.ancho = ancho;\n    this.alto = alto;\n  }\n  area() { return this.ancho * this.alto; }\n}\nclass Cuadrado extends Rectangulo {\n  constructor(lado) {\n    super(lado, lado);\n  }\n}\nconst figura = new Cuadrado(5);", "extends conecta la cadena prototípica y super inicializa la parte Rectangulo antes de completar la instancia derivada.", [test("class-area", "Hereda el cálculo de área", "figura.area()", 25), test("class-derived", "Es instancia derivada", "figura instanceof Cuadrado", true), test("class-base", "También respeta la clase base", "figura instanceof Rectangulo", true)], ["mdn-classes", "mdn-prototype-chain", "ecma-262"]),
  exercise(7, "difficult-accessor-range", "Validar con getter y setter", "getters-setters", "difficult-getters-setters", "Completa Nivel para guardar un valor entre 0 y 100. Usa _valor como almacenamiento y expón valor mediante getter y setter.", "class Nivel {\n  constructor() { this._valor = 0; }\n  get valor() {\n    // Completa\n  }\n  set valor(nuevo) {\n    // Limita el rango\n  }\n}\nconst nivel = new Nivel();", ["El getter devuelve _valor para no llamarse a sí mismo.", "Combina Math.max y Math.min para limitar nuevo."], "class Nivel {\n  constructor() { this._valor = 0; }\n  get valor() {\n    return this._valor;\n  }\n  set valor(nuevo) {\n    this._valor = Math.max(0, Math.min(100, nuevo));\n  }\n}\nconst nivel = new Nivel();", "El descriptor accesor controla la lectura y asignación públicas, mientras _valor evita recursión dentro de ambos accesores.", [test("setter-high", "Limita el máximo", "(nivel.valor = 140, nivel.valor)", 100), test("setter-low", "Limita el mínimo", "(nivel.valor = -8, nivel.valor)", 0), test("setter-middle", "Conserva valores válidos", "(nivel.valor = 42, nivel.valor)", 42)], ["mdn-descriptors", "mdn-classes", "ecma-262"]),
  exercise(8, "difficult-custom-iterator", "Construir un iterable", "iteration", "difficult-iterables-iterators", "Implementa intervalo(desde, hasta) como iterable inclusivo. Cada recorrido debe obtener un iterator nuevo e independiente.", "function intervalo(desde, hasta) {\n  return {\n    [Symbol.iterator]() {\n      // Devuelve un iterator\n    },\n  };\n}\nconst numeros = intervalo(3, 5);", ["Declara actual dentro de [Symbol.iterator].", "next devuelve { value, done: false } mientras actual <= hasta y luego { done: true }."], "function intervalo(desde, hasta) {\n  return {\n    [Symbol.iterator]() {\n      let actual = desde;\n      return {\n        next() {\n          if (actual <= hasta) return { value: actual++, done: false };\n          return { done: true };\n        },\n      };\n    },\n  };\n}\nconst numeros = intervalo(3, 5);", "El estado actual pertenece a cada iterator. Spread puede recorrer el mismo iterable dos veces sin compartir un iterator agotado.", [test("iterator-values", "Produce el intervalo inclusivo", "[...numeros]", [3, 4, 5], "deepEqual"), test("iterator-repeat", "Crea recorridos independientes", "[[...numeros], [...numeros]]", [[3, 4, 5], [3, 4, 5]], "deepEqual")], ["mdn-iteration", "mdn-symbol", "ecma-262"]),
  exercise(9, "difficult-generator-sequence", "Secuencia con generator", "generators", "difficult-generators", "Implementa pasos(inicio, cantidad) como generator que entregue cantidad números consecutivos desde inicio.", "function* pasos(inicio, cantidad) {\n  // Usa yield dentro de un bucle\n}\nconst secuencia = pasos(4, 3);", ["Un generator se suspende en cada yield.", "Itera desplazamiento desde 0 mientras sea menor que cantidad."], "function* pasos(inicio, cantidad) {\n  for (let desplazamiento = 0; desplazamiento < cantidad; desplazamiento += 1) {\n    yield inicio + desplazamiento;\n  }\n}\nconst secuencia = pasos(4, 3);", "yield entrega cada valor sin construir primero un array, y el generator conserva el desplazamiento entre llamadas a next().", [test("generator-values", "Entrega tres pasos", "[...secuencia]", [4, 5, 6], "deepEqual"), test("generator-new", "Permite una secuencia nueva", "[...pasos(8, 2)]", [8, 9], "deepEqual")], ["mdn-iteration", "ecma-262"]),
  exercise(10, "difficult-map-set-index", "Indexar con Map y Set", "map-set", "difficult-map-set", "Implementa resumir(etiquetas): devuelve un Map con la frecuencia de cada etiqueta y un Set con las etiquetas únicas.", "function resumir(etiquetas) {\n  // Devuelve { frecuencias, unicas }\n}\nconst resumen = resumir([\"js\", \"web\", \"js\"]);", ["Usa frecuencias.get(etiqueta) ?? 0 antes de sumar.", "Set puede construirse directamente desde etiquetas."], "function resumir(etiquetas) {\n  const frecuencias = new Map();\n  for (const etiqueta of etiquetas) {\n    frecuencias.set(etiqueta, (frecuencias.get(etiqueta) ?? 0) + 1);\n  }\n  const unicas = new Set(etiquetas);\n  return { frecuencias, unicas };\n}\nconst resumen = resumir([\"js\", \"web\", \"js\"]);", "Map expresa el índice de frecuencias y Set el contrato de unicidad sin convertir claves ni buscar repetidamente en un array.", [test("map-count", "Cuenta etiquetas repetidas", "resumen.frecuencias.get('js')", 2), test("map-other", "Cuenta otra etiqueta", "resumen.frecuencias.get('web')", 1), test("set-values", "Conserva únicas en orden", "[...resumen.unicas]", ["js", "web"], "deepEqual")], ["mdn-keyed-collections", "ecma-262"]),
  exercise(11, "difficult-promise-combinators", "Coordinar resultados de Promises", "promise-combinators", "difficult-promise-combinators", "Obtén valores con Promise.all para 2 y 4. Usa Promise.allSettled para registrar un fulfillment 'ok' y un rechazo 'fallo' sin perder ninguno.", "// Declara valores y estados mediante await", ["Promise.all conserva el orden de entrada.", "allSettled cumple con descriptores status/value o status/reason."], "const valores = await Promise.all([Promise.resolve(2), Promise.resolve(4)]);\nconst estados = await Promise.allSettled([\n  Promise.resolve(\"ok\"),\n  Promise.reject(\"fallo\"),\n]);", "all exige que ambas operaciones cumplan; allSettled espera que ambas se asienten y permite inspeccionar por separado cumplimiento y rechazo.", [test("promise-all", "Reúne todos los valores", "valores", [2, 4], "deepEqual"), test("settled-status", "Distingue estados", "estados.map((estado) => estado.status)", ["fulfilled", "rejected"], "deepEqual"), test("settled-values", "Conserva valor y razón", "[estados[0].value, estados[1].reason]", ["ok", "fallo"], "deepEqual")], ["mdn-promise-combinators", "ecma-262"]),
  exercise(12, "difficult-microtask-order", "Ordenar continuaciones sin timers", "microtasks-tasks", "difficult-microtasks-macrotasks", "Construye orden A, D, C, B sin timers: agrega A, encola C con Promise.then, agrega D, espera una Promise resuelta y agrega B.", "const orden = [];\n// Completa el orden solicitado", ["El callback de then es una microtask.", "Cuando await programa su continuación, la microtask de C ya está en la cola."], "const orden = [];\norden.push(\"A\");\nPromise.resolve().then(() => orden.push(\"C\"));\norden.push(\"D\");\nawait Promise.resolve();\norden.push(\"B\");", "A y D son síncronos. La reacción que agrega C se encola antes de la continuación del await, por eso el resultado determinista es A, D, C, B sin depender de timers del host.", [test("microtask-order", "Respeta el orden de microtasks", "orden", ["A", "D", "C", "B"], "deepEqual")], ["mdn-execution-model", "ecma-262"]),
  exercise(13, "difficult-async-errors", "Transformar errores asíncronos", "async-await", "difficult-async-await", "Implementa leerSeguro(operacion): espera la operación, devuelve su valor; ante rechazo devuelve el string 'recuperado: ' seguido del mensaje.", "async function leerSeguro(operacion) {\n  // Usa try/catch y await\n}\nconst exito = await leerSeguro(Promise.resolve(7));\nconst recuperado = await leerSeguro(Promise.reject(new Error(\"sin dato\")));", ["await debe estar dentro del try para que catch reciba el rechazo.", "El error capturado expone message."], "async function leerSeguro(operacion) {\n  try {\n    return await operacion;\n  } catch (error) {\n    return `recuperado: ${error.message}`;\n  }\n}\nconst exito = await leerSeguro(Promise.resolve(7));\nconst recuperado = await leerSeguro(Promise.reject(new Error(\"sin dato\")));", "await convierte el rechazo en una excepción dentro de leerSeguro. El catch define una recuperación explícita, por lo que la Promise resultante cumple con el mensaje alternativo.", [test("async-success", "Conserva un fulfillment", "exito", 7), test("async-recovery", "Recupera un rechazo", "recuperado", "recuperado: sin dato"), test("async-reuse", "Maneja otro error", "await leerSeguro(Promise.reject(new Error('x')))", "recuperado: x")], ["mdn-async-function", "ecma-262"]),
  exercise(14, "difficult-composition-memoization", "Componer y memoizar", "composition-memoization", "difficult-composition-memoization", "Implementa componer(f, g) y memoizar(fn) con Map. La función memoizada debe reutilizar el resultado para la misma clave primitiva.", "const componer = // Completa\nfunction memoizar(fn) {\n  // Completa con Map\n}\nlet calculos = 0;\nconst doble = memoizar((n) => { calculos += 1; return n * 2; });", ["componer devuelve una función que calcula f(g(valor)).", "En memoizar, distingue cache.has(argumento) de obtener un valor falsy."], "const componer = (f, g) => (valor) => f(g(valor));\nfunction memoizar(fn) {\n  const cache = new Map();\n  return function (argumento) {\n    if (cache.has(argumento)) return cache.get(argumento);\n    const resultado = fn(argumento);\n    cache.set(argumento, resultado);\n    return resultado;\n  };\n}\nlet calculos = 0;\nconst doble = memoizar((n) => { calculos += 1; return n * 2; });", "La composición conecta contratos pequeños. La cache de Map evita repetir fn para una clave ya vista; con objetos, la coincidencia sería por identidad y la retención de claves tendría costo de memoria.", [test("compose", "Compone en el orden correcto", "componer((n) => n + 1, (n) => n * 2)(3)", 7), test("memo-values", "Devuelve el valor memoizado", "[doble(4), doble(4), doble(5)]", [8, 8, 10], "deepEqual"), test("memo-calls", "Evita un cálculo repetido", "calculos", 2)], ["mdn-closures", "mdn-keyed-collections", "ecma-262"]),
];
