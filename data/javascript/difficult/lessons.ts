import type { Lesson } from "@/types/learning";

const lesson = (
  id: string,
  title: string,
  concept: string,
  explanation: readonly string[],
  code: string,
  keyPoints: readonly string[],
  sourceIds: readonly string[],
  exerciseId?: string,
): Lesson => ({ id, slug: id, title, concept, difficulty: "Difícil", explanation, examples: [{ label: "Ejemplo", code }], keyPoints, sourceIds, exerciseId });

export const difficultLessons: readonly Lesson[] = [
  lesson("difficult-closures", "Closures", "closures", [
    "Un closure es una función junto con el acceso al entorno léxico donde fue creada. Conserva bindings, no copias mágicas de sus valores: si el binding cambia, las llamadas posteriores observan ese estado actualizado.",
    "Este mecanismo permite encapsular estado y fabricar funciones configuradas. Amplía el alcance estudiado antes: la vida de un binding puede superar la ejecución que creó su entorno cuando una función todavía lo referencia.",
  ], "function crearSecuencia(inicio) {\n  let actual = inicio;\n  return () => actual++;\n}\nconst siguiente = crearSecuencia(10);\nconsole.log(siguiente(), siguiente());", ["Cada llamada exterior crea bindings independientes.", "Error común: describir el closure como una fotografía inmutable del valor.", "La encapsulación depende del alcance léxico, no de una palabra reservada especial."], ["mdn-closures", "ecma-262"], "difficult-closure-counter"),
  lesson("difficult-lexical-environment", "Lexical environment y lexical scope avanzado", "lexical-environment", [
    "La estructura léxica del código determina qué entorno exterior consulta un nombre. Durante la ejecución, los registros de entorno mantienen bindings para parámetros, declaraciones y bloques, y se enlazan con un entorno externo.",
    "Una función busca nombres desde el lugar donde fue definida, no desde donde luego se invoca. Esto explica closures y sombreado sin imaginar objetos públicos que puedan inspeccionarse directamente.",
  ], "const etiqueta = \"exterior\";\nfunction crear() {\n  const etiqueta = \"léxica\";\n  return () => etiqueta;\n}\nconsole.log(crear()());", ["El scope se decide por la estructura del programa.", "Error común: esperar que el caller aporte sus variables locales.", "El sombreado crea otro binding; no modifica automáticamente el exterior."], ["mdn-closures", "ecma-262"]),
  lesson("difficult-this", "this y call-site", "this", [
    "En una función normal, el valor de this depende de cómo se realiza la llamada: como método, con new, mediante call/apply/bind o como llamada simple. La función no queda unida para siempre al objeto donde fue escrita.",
    "Una arrow function no crea su propio this: captura el binding léxico del entorno. Por eso call, apply y bind pueden fijar this en funciones normales, pero no reemplazan el this capturado por una arrow.",
  ], "function leer() { return this.nombre; }\nconst uno = { nombre: \"uno\", leer };\nconst dos = { nombre: \"dos\", leer };\nconsole.log(uno.leer(), dos.leer());", ["Observa el call-site, no sólo la declaración.", "Error común: separar un método y esperar que conserve su receptor.", "Las arrows sirven para heredar this, no como reemplazo universal de method syntax."], ["mdn-this", "ecma-262"], "difficult-this-call-site"),
  lesson("difficult-call-apply-bind", "call, apply y bind", "call-apply-bind", [
    "call y apply invocan una función normal de inmediato con un this explícito; call recibe argumentos separados y apply recibe una colección de argumentos. bind crea otra función que conserva el this y, opcionalmente, argumentos iniciales.",
    "Estas operaciones hacen explícito el receptor cuando una función debe reutilizarse o viajar como callback. Sobre una arrow no sustituyen el this léxico, aunque bind todavía puede prefijar argumentos.",
  ], "function presentar(prefijo, cierre) {\n  return `${prefijo} ${this.nombre}${cierre}`;\n}\nconst contexto = { nombre: \"Ada\" };\nconsole.log(presentar.call(contexto, \"Hola\", \".\"));", ["call y apply ejecutan; bind devuelve una función.", "Error común: pasar un array a call cuando corresponde apply o spread.", "Relaciona estas APIs con el call-site de la lección anterior."], ["mdn-this", "ecma-262"], "difficult-bind-context"),
  lesson("difficult-prototype-chain", "Prototype chain", "prototype-chain", [
    "Cuando una propiedad no es propia, JavaScript continúa la búsqueda por el prototipo del objeto y repite el proceso hasta encontrarla o llegar a null. Esa cadena delega comportamiento sin copiar cada método en cada instancia.",
    "Object.getPrototypeOf permite observar el enlace; hasOwn distingue una propiedad propia de una heredada. La asignación normal suele crear o actualizar una propiedad propia y puede ocultar la heredada.",
  ], "const base = { describir() { return this.nombre.toUpperCase(); } };\nconst elemento = Object.create(base);\nelemento.nombre = \"nodo\";\nconsole.log(elemento.describir());", ["La consulta sube por la cadena; la identidad del receptor sigue siendo elemento.", "Error común: creer que el método heredado fue copiado al objeto.", "Este modelo es la base sobre la que operan las clases."], ["mdn-prototype-chain", "ecma-262"], "difficult-prototype-method"),
  lesson("difficult-prototypal-inheritance", "Herencia prototípica", "prototypal-inheritance", [
    "La herencia prototípica conecta objetos para delegar lecturas y métodos. Los constructores tradicionales exponen un objeto prototype que se convierte en prototipo de las instancias creadas con new.",
    "Compartir métodos en el prototipo reduce duplicación, pero compartir allí estado mutable mezcla accidentalmente instancias. Cada instancia debe poseer los datos que cambian de manera independiente.",
  ], "function Cuenta(nombre) { this.nombre = nombre; this.movimientos = []; }\nCuenta.prototype.registrar = function (valor) { this.movimientos.push(valor); };\nconst cuenta = new Cuenta(\"Caja\");", ["prototype del constructor no es lo mismo que el prototipo del constructor como función.", "Error común: colocar arrays mutables compartidos en el prototipo.", "Object.create ofrece delegación directa sin constructor."], ["mdn-prototype-chain", "ecma-262"]),
  lesson("difficult-property-descriptors", "Property descriptors", "property-descriptors", [
    "Una propiedad de datos tiene un valor y atributos writable, enumerable y configurable. Una propiedad accesora usa get y/o set en lugar de value y writable; Object.getOwnPropertyDescriptor permite inspeccionar ese contrato.",
    "Object.defineProperty usa false como valor predeterminado para atributos omitidos. Los descriptores controlan operaciones del lenguaje, no validan por sí solos el significado del dato.",
  ], "const registro = {};\nObject.defineProperty(registro, \"id\", {\n  value: 7, enumerable: true, writable: false, configurable: false,\n});\nconsole.log(Object.keys(registro));", ["No mezcles value/writable con get/set en el mismo descriptor.", "Error común: asumir que defineProperty replica los defaults de un literal.", "La enumerabilidad afecta Object.keys, no la existencia de la propiedad."], ["mdn-descriptors", "ecma-262"]),
  lesson("difficult-getters-setters", "Getters y setters", "getters-setters", [
    "Un getter calcula una lectura con sintaxis de propiedad y un setter intercepta una asignación. Son accesores síncronos: conviene que su costo y efectos no sorprendan a quien usa el objeto.",
    "El almacenamiento debe usar otro nombre o un campo privado. Escribir this.valor dentro del setter de valor volvería a invocar el mismo setter hasta desbordar la pila.",
  ], "const rango = {\n  _valor: 0,\n  get valor() { return this._valor; },\n  set valor(nuevo) { this._valor = Math.max(0, Math.min(10, nuevo)); },\n};\nrango.valor = 14;", ["Los accesores se describen con get/set, no value.", "Error común: recursión al leer o escribir la misma propiedad dentro del accesor.", "Amplían los descriptores con una interfaz de propiedad."], ["mdn-descriptors", "ecma-262"], "difficult-accessor-range"),
  lesson("difficult-classes", "Classes", "classes", [
    "La sintaxis class organiza constructor, métodos, accesores y campos. Es una abstracción sobre el modelo de prototypes: los métodos de instancia se ubican en el prototype y las instancias delegan hacia él.",
    "Las clases añaden reglas propias, como ejecución estricta y la obligación de usar new. La sintaxis resulta más declarativa, pero no convierte JavaScript en un sistema de clases desconectado de su cadena prototípica.",
  ], "class Medidor {\n  constructor(unidad) { this.unidad = unidad; this.valor = 0; }\n  sumar(cantidad) { this.valor += cantidad; return this.valor; }\n}\nconst medidor = new Medidor(\"km\");", ["Los métodos se comparten mediante Medidor.prototype.", "Error común: pensar que cada instancia recibe una copia de cada método.", "Comprender prototypes permite explicar instanceof y herencia de clases."], ["mdn-classes", "mdn-prototype-chain", "ecma-262"]),
  lesson("difficult-class-inheritance", "Herencia de clases: extends y super", "class-inheritance", [
    "extends enlaza tanto la cadena de instancias como la relación estática entre clases. En un constructor derivado, super(...) debe ejecutar el constructor base antes de acceder a this.",
    "super.metodo() permite extender una implementación base sin buscarla manualmente. La herencia expresa una relación de sustitución; para combinar capacidades independientes suele ser más claro componer objetos.",
  ], "class Mensaje { constructor(texto) { this.texto = texto; } formato() { return this.texto; } }\nclass Alerta extends Mensaje {\n  constructor(texto) { super(texto); }\n  formato() { return `¡${super.formato()}!`; }\n}", ["super() inicializa this en constructores derivados.", "Error común: usar this antes de super().", "extends conserva el modelo prototípico en lugar de reemplazarlo."], ["mdn-classes", "mdn-prototype-chain", "ecma-262"], "difficult-class-inheritance"),
  lesson("difficult-private-fields", "Private fields", "private-fields", [
    "Los nombres #privados se declaran en el cuerpo de la clase y sólo pueden referenciarse con esa sintaxis dentro de la clase. Son elementos privados reales con comprobación de marca (brand check), no propiedades string ocultas.",
    "objeto['#saldo'] no accede al campo #saldo y Object.keys no lo enumera. Intentar usar un private name no declarado es un error de sintaxis; aplicarlo a un objeto sin la marca correcta produce un error.",
  ], "class Billetera {\n  #saldo = 0;\n  depositar(monto) { this.#saldo += monto; }\n  consultar() { return this.#saldo; }\n}\nconst billetera = new Billetera();", ["La privacidad se valida por sintaxis y marca de instancia.", "Error común: confundir #dato con la clave string '#dato'.", "Los closures también encapsulan, pero con un mecanismo y forma de compartir distintos."], ["mdn-private-elements", "mdn-classes", "ecma-262"]),
  lesson("difficult-symbols", "Symbols", "symbols", [
    "Cada Symbol() crea un valor primitivo único que puede usarse como clave de propiedad sin colisionar con claves string. Su descripción ayuda a depurar, pero dos símbolos con la misma descripción siguen siendo diferentes.",
    "Los well-known symbols permiten participar en protocolos del lenguaje, como Symbol.iterator. Object.keys omite claves symbol; Reflect.ownKeys incluye claves string y symbol.",
  ], "const estado = Symbol(\"estado\");\nconst tarea = { nombre: \"revisar\", [estado]: \"interna\" };\nconsole.log(Object.keys(tarea), Reflect.ownKeys(tarea));", ["Symbol no vuelve secreta una propiedad: puede descubrirse con APIs de reflexión.", "Error común: comparar símbolos por su descripción.", "Symbol.iterator conecta este tema con el protocolo iterable."], ["mdn-symbol", "ecma-262"]),
  lesson("difficult-iterables-iterators", "Iterables e iterators", "iteration", [
    "Un iterable implementa [Symbol.iterator]() y devuelve un iterator. Cada llamada a next() del iterator produce un objeto con value y done; for...of y spread consumen ese protocolo.",
    "Separar iterable de iterator permite crear recorridos nuevos y, si se desea, varios iterators independientes. Un iterator agotado debe continuar informando done: true.",
  ], "const tramo = {\n  desde: 2, hasta: 4,\n  [Symbol.iterator]() {\n    let actual = this.desde; const fin = this.hasta;\n    return { next: () => actual <= fin ? { value: actual++, done: false } : { done: true } };\n  },\n};\nconsole.log([...tramo]);", ["Iterable inicia recorridos; iterator entrega pasos.", "Error común: devolver valores directos en next() en vez de { value, done }.", "for...of consume valores y no depende de índices."], ["mdn-iteration", "mdn-symbol", "ecma-262"], "difficult-custom-iterator"),
  lesson("difficult-generators", "Generators", "generators", [
    "Una función generator se declara con function* y devuelve un generator que implementa los protocolos iterator e iterable. Cada yield suspende su ejecución y conserva su estado hasta el siguiente next().",
    "El valor pasado a next(valor) se convierte en el resultado del yield suspendido, salvo en la primera llamada. return finaliza la secuencia y su valor no aparece normalmente en for...of.",
  ], "function* alternar() {\n  yield \"izquierda\";\n  yield \"derecha\";\n}\nconsole.log([...alternar()]);", ["Invocar la función no ejecuta inmediatamente todo su cuerpo.", "Error común: reutilizar un generator agotado esperando que reinicie.", "Los generators reducen el estado manual de muchos iterators."], ["mdn-iteration", "ecma-262"], "difficult-generator-sequence"),
  lesson("difficult-map-set", "Map y Set", "map-set", [
    "Map asocia claves de cualquier tipo con valores y conserva orden de inserción para iteración. Set mantiene valores únicos con el mismo criterio de igualdad usado por estas colecciones.",
    "Frente a un objeto, Map evita convertir claves a string y expone size e iteración directa. Set elimina duplicados por identidad para objetos: dos literales equivalentes siguen siendo valores distintos.",
  ], "const visitas = new Map();\nconst pagina = { ruta: \"/inicio\" };\nvisitas.set(pagina, 2);\nconst etiquetas = new Set([\"js\", \"js\", \"web\"]);", ["Map usa get/set/has; no notación de punto para sus entradas.", "Error común: esperar igualdad profunda en Set.", "Estas colecciones hacen explícitos contratos que antes se simulaban con objetos y arrays."], ["mdn-keyed-collections", "ecma-262"], "difficult-map-set-index"),
  lesson("difficult-weakmap-weakset", "WeakMap y WeakSet", "weak-collections", [
    "WeakMap mantiene asociaciones cuyas claves deben ser objetos o símbolos no registrados; WeakSet mantiene referencias débiles admitidas por su contrato. No impiden por sí solos que una clave recolectable sea liberada.",
    "No son enumerables y no exponen size porque observar sus miembros haría depender el programa de decisiones de recolección de basura. Las pruebas deben verificar el comportamiento de has/get/delete, nunca afirmar que ocurrió GC.",
  ], "const metadatos = new WeakMap();\nconst elemento = {};\nmetadatos.set(elemento, { validado: true });\nconsole.log(metadatos.get(elemento).validado);", ["Su caso típico es asociar metadata sin controlar la vida de la clave.", "Error común: intentar recorrer una colección weak.", "La recolección es no determinista y no constituye una aserción válida."], ["mdn-keyed-collections", "ecma-262"]),
  lesson("difficult-promise-combinators", "Promise combinators", "promise-combinators", [
    "Promise.all cumple con todos los valores en orden y rechaza al primer rechazo observado. allSettled siempre cumple cuando todas se asentaron y describe cada fulfillment o rejection.",
    "Promise.race se asienta como la primera entrada que se asiente; Promise.any cumple con el primer fulfillment y sólo rechaza con AggregateError si todas rechazan. Settlement incluye cumplimiento o rechazo, no sólo finalización exitosa.",
  ], "const datos = await Promise.all([Promise.resolve(3), 5]);\nconst estados = await Promise.allSettled([Promise.resolve(1), Promise.reject(\"x\")]);\nconsole.log(datos, estados.map((item) => item.status));", ["Los combinadores no cancelan automáticamente las operaciones restantes.", "Error común: confundir race con primer fulfillment; ese contrato corresponde a any.", "Amplían las cadenas Promise del nivel medio hacia coordinación explícita."], ["mdn-promise-combinators", "ecma-262"], "difficult-promise-combinators"),
  lesson("difficult-event-loop", "Event loop y modelo de ejecución", "event-loop", [
    "El stack ejecuta un job a la vez. Operaciones del host pueden registrar trabajo y, cuando corresponde, colocar callbacks en colas; el event loop del host elige la siguiente task cuando el stack queda libre.",
    "ECMAScript especifica jobs, como reacciones de Promise, mientras navegador y Node definen tasks, timers y fases. Por eso conviene razonar stack → host → queues → event loop y separar garantías del lenguaje de detalles específicos del host.",
  ], "console.log(\"A\");\nsetTimeout(() => console.log(\"B\"), 0);\nPromise.resolve().then(() => console.log(\"C\"));\nconsole.log(\"D\");\n// Orden verificado: A, D, C, B", ["El código síncrono A y D termina antes de ejecutar callbacks.", "Error común: creer que un timer con cero demora interrumpe el stack.", "Node posee fases y prioridades propias que no deben presentarse como reglas universales de ECMAScript."], ["mdn-execution-model", "node-event-loop", "ecma-262"]),
  lesson("difficult-microtasks-macrotasks", "Microtasks y tasks", "microtasks-tasks", [
    "Las reacciones de Promise y queueMicrotask se encolan como microtasks del host. Al terminar la ejecución actual, el host drena la cola de microtasks antes de avanzar a la siguiente task, incluido un callback de timer.",
    "Una microtask puede encolar otras microtasks, de modo que una cadena sin límite puede retrasar tasks. El nombre macrotask es informal; la terminología de plataforma suele hablar simplemente de tasks.",
  ], "const orden = [];\norden.push(\"A\");\nqueueMicrotask(() => orden.push(\"C\"));\norden.push(\"D\");\nawait Promise.resolve();\nconsole.log(orden.join(\" \"));", ["Las microtasks se drenan antes de la próxima task.", "Error común: atribuir una prioridad de microtask a todo callback asíncrono.", "El orden A, D, C, B combina ejecución síncrona, microtask y task del host."], ["mdn-execution-model", "node-event-loop", "ecma-262"], "difficult-microtask-order"),
  lesson("difficult-async-await", "Async/await avanzado", "async-await", [
    "Cada llamada async devuelve una Promise. await obtiene un fulfillment o lanza el motivo de rechazo dentro de la función, y la continuación posterior se programa como un job en lugar de bloquear el hilo.",
    "Esperar operaciones independientes una por una puede serializarlas innecesariamente. Iniciarlas primero y coordinarlas con Promise.all conserva concurrencia; hacerlo sólo es correcto cuando no existe dependencia entre resultados.",
  ], "async function resumen() {\n  const perfilPendiente = Promise.resolve({ id: 4 });\n  const permisosPendientes = Promise.resolve([\"leer\"]);\n  const [perfil, permisos] = await Promise.all([perfilPendiente, permisosPendientes]);\n  return { perfil, permisos };\n}", ["await suspende la función async, no todo el entorno.", "Error común: serializar trabajo independiente con awaits consecutivos.", "El manejo de rechazo debe ubicarse donde exista una recuperación útil."], ["mdn-async-function", "mdn-promise-combinators", "ecma-262"], "difficult-async-errors"),
  lesson("difficult-error-propagation", "Propagación de errores asíncronos", "error-propagation", [
    "Un throw dentro de async rechaza la Promise devuelta. Un rechazo esperado por await vuelve a lanzarse en esa función; si no se captura, continúa por la cadena hasta un manejador de rechazo.",
    "Capturar sólo para registrar y devolver undefined puede ocultar el contrato fallido. Es preferible recuperar con un valor válido, agregar contexto mediante cause o propagar el error para que una capa responsable decida.",
  ], "async function cargar() {\n  try {\n    return await Promise.reject(new Error(\"dato ausente\"));\n  } catch (cause) {\n    throw new Error(\"No se pudo cargar el perfil\", { cause });\n  }\n}", ["Un catch transforma el resultado si devuelve otro valor.", "Error común: olvidar await dentro de try y dejar el rechazo fuera de ese catch.", "La propagación asíncrona conserva la idea de contratos de error del nivel medio."], ["mdn-async-function", "ecma-262"]),
  lesson("difficult-modules", "Módulos ECMAScript", "modules", [
    "Los módulos tienen su propio ámbito, dependencias declaradas y bindings importados vivos: una importación observa actualizaciones del binding exportado, aunque no permite reasignarlo desde el importador. La resolución y carga pertenecen al host.",
    "Este playground bloquea import y export porque ejecuta fragmentos confinados sin cargar módulos, archivos o red. La lección es exclusivamente teórica: no se habilitan módulos ni se evade esa frontera para practicar la sintaxis.",
  ], "// contador.js\nexport let total = 0;\nexport function incrementar() { total += 1; }\n// vista.js\nimport { total, incrementar } from \"./contador.js\";", ["Los imports estáticos se declaran en el nivel superior.", "Error común: describir un binding importado como una copia fija del valor.", "No pegues este ejemplo en el playground: import/export están bloqueados deliberadamente."], ["mdn-modules", "ecma-262"]),
  lesson("difficult-immutability", "Inmutabilidad y Object.freeze", "immutability", [
    "Object.freeze impide reconfigurar propiedades propias y vuelve no escribibles las propiedades de datos del objeto. Es superficial: un objeto o array anidado conserva su mutabilidad salvo que también se congele.",
    "Actualizar mediante copias puede facilitar razonamiento y detección de cambios, pero spread también es superficial. La inmutabilidad profunda requiere recorrer la estructura o adoptar contratos que eviten exponer referencias mutables.",
  ], "const configuracion = Object.freeze({ tema: \"claro\", opciones: { compacto: false } });\nconfiguracion.opciones.compacto = true;\nconsole.log(configuracion.opciones.compacto);", ["freeze devuelve el mismo objeto.", "Error común: asumir que congela recursivamente todo el grafo.", "Conecta con referencias y copias superficiales del nivel medio."], ["mdn-object-freeze", "ecma-262"]),
  lesson("difficult-composition-memoization", "Composición y memoization", "composition-memoization", [
    "La composición construye una operación conectando funciones pequeñas, con salidas compatibles con las entradas siguientes. Reduce jerarquías cuando el comportamiento puede combinarse sin una relación de herencia.",
    "Memoization guarda resultados por entrada para evitar trabajo repetido, pero introduce tradeoffs: costo de cache, semántica de identidad para objetos, invalidación y retención de memoria. Sólo conviene para funciones compatibles con reutilizar el resultado y cargas donde la cache aporta valor.",
  ], "const componer = (f, g) => (valor) => f(g(valor));\nconst normalizar = (texto) => texto.trim().toLowerCase();\nconst longitud = (texto) => texto.length;\nconst longitudNormalizada = componer(longitud, normalizar);", ["La composición exige contratos claros entre funciones.", "Error común: memoizar funciones con efectos o resultados dependientes del tiempo.", "Map usa identidad de claves; esa decisión afecta aciertos y memoria de una cache."], ["mdn-closures", "mdn-keyed-collections", "ecma-262"], "difficult-composition-memoization"),
];
